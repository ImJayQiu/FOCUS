<%= form_with(model: rmethod, local: true) do |form| %>
  <% if rmethod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rmethod.errors.count, "error") %> prohibited this rmethod from being saved:</h2>

      <ul>
        <% rmethod.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :fname %>
    <%= form.text_field :fname %>
  </div>

  <div class="field">
    <%= form.label :remark %>
    <%= form.text_field :remark %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
